# Example environment variables for backend
# Use SQLite by default (no change needed)
# To use Postgres, set DATABASE_URL to something like:
# postgresql+psycopg2://user:password@localhost:5432/researchdb

DATABASE_URL=sqlite:///./backend.db
